import * as React from 'react';
declare const Modal: ({ open, close, children, width }: {
    open: any;
    close: any;
    children: any;
    width?: string;
}) => React.ReactPortal;
export default Modal;
